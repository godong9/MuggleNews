//timeline-controller.js
define([
], function (
) {
  'use strict';

  function TimelineController(model, view) {
    let self = this;
    self.model = model;
    self.view = view;
  }

  return TimelineController;
});